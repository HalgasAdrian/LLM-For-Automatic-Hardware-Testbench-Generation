# ## Environment Setup

### 1. Select GPU runtime  
In Colab’s menu go to  
`Runtime → Change runtime type → Hardware accelerator: GPU`  

### 2. Install Python & system deps  
```bash
# Python packages
!pip install \
  torch>=2.0.0 \
  transformers>=4.30.0 \
  peft>=0.4.0 \
  bitsandbytes>=0.39.0 \
  accelerate>=0.21.0 \
  datasets>=2.6.0 \
  tqdm>=4.65.0 \
  pyverilog>=1.4.0 \
  pyverilator>=0.4.0 \
  numpy>=1.24.0

# Verilog simulators
!apt-get update && apt-get install -y iverilog verilator
